@using AdvantShop.Core.UrlRewriter
@using AdvantShop.Module.RemindAboutReceipt.Service
@using AdvantShop.Web.Infrastructure.Extensions;
@*GlorySoft_012*@

@model AdvantShop.Catalog.Offer
@{ var url = UrlService.GetUrl();
                var scripts = new List<string>
{
        url + "modules/" + ModuleSettings.ModuleID + "/content/scripts/showNotificationForm/controllers/productViewController.js"
    };
                var version = Html.GetModuleVersion(ModuleSettings.ModuleID); }

<div @*data-oc-lazy-load="[@String.Join(" ,", scripts.Select(x=>"'" + x + "?" + version + "'" ))]"*@>
    <div>
        @*id="RemindModuleShowNotificationFormBlock"> @*class="btn btn-small btn-action icon-bag-before products-view-buy">*@
        @*<div class="modal-content">
                <rar-product-view data-product-id="@Model.ProductId" data-offer-id="@Model.ArtNo"></rar-product-view>
            </div>*@
        <a href="products/@Model.Product.UrlPath" class="btn btn-small btn-action rar-product-view">
            @T("RARModule.ProductView.Button")
        </a>
    </div>
</div>